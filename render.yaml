services:
  - type: web
    name: yt-mp3-converter
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: YT_COOKIES
        sync: false  # Set manually in Render dashboard (base64 encoded cookies.txt)
